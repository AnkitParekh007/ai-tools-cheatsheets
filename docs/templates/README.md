# Templates Overview

Copy these templates into team repositories and tailor them to the local codebase.

> Team tip: Start with `AGENTS.md`, then add `CLAUDE.md`, Cursor rules, and governance templates only where they add real value.
