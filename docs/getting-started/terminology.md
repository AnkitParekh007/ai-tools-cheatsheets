# Terminology

- `CLI agent`: tool used primarily from the terminal
- `IDE agent`: tool embedded inside an editor
- `model provider`: vendor supplying the underlying LLM
- `instruction file`: repo or tool file that gives persistent guidance
- `MCP`: protocol connecting models to tools and external context
